var $carousel = $('.carousel').flickity({
    // options
    cellAlign: 'center',
    freeScroll: true,
    // contain: true,
    // percentPosition: false,
    // prevNextButtons: false,
    pageDots: false
});
